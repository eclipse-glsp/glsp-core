# do not export the DI utilities to avoid a necessary dependency on reflect-metadata/inversify
# Available via the `@eclipse-glsp/protocol/di` entry point (see `exports` in package.json) and reexported in
# @eclipse-glsp/client & @eclipse-glsp/server
di